doctype html
head
  meta(charset="UTF-8")
  title MY APP

body May the Node and Express be with you.
  ul(class="quotes")
    each quote in quotes
      li(class="quote")
        span=quote.name + ' ' + quote.quote

  form
    input(type="text" placeholder="name" name"name")
    input(type="text" placeholder="quote" name="quote")
    button(type="submit") Submit
